title: Best Time to Buy and Sell Stock
slug: best-time-to-buy-and-sell-stock
difficulty: EASY
category: ALGORITHM
tags: [array, dynamic-programming]
concepts:
  - name: Array
    relevance: 0.8
  - name: Dynamic Programming
    relevance: 0.5
description: |
  You are given an array `prices` where `prices[i]` is the price of a given stock on the `i`th day.

  You want to maximize your profit by choosing a **single day** to buy one stock and choosing a **different day in the future** to sell that stock.

  Return the maximum profit you can achieve from this transaction. If you cannot achieve any profit, return `0`.

  **Example 1:**
  ```
  Input: prices = [7,1,5,3,6,4]
  Output: 5
  Explanation: Buy on day 2 (price = 1) and sell on day 5 (price = 6), profit = 6 - 1 = 5.
               Note that buying on day 2 and selling on day 1 is not allowed because you must buy before you sell.
  ```

  **Example 2:**
  ```
  Input: prices = [7,6,4,3,1]
  Output: 0
  Explanation: In this case, no transactions are done and the max profit = 0.
  ```

  **Constraints:**
  - `1 <= prices.length <= 10^5`
  - `0 <= prices[i] <= 10^4`
hints:
  - Think about a simpler question: if you sell on day i, which day should you have bought to maximize profit?
  - Traverse from left to right while maintaining a variable tracking the "lowest price seen so far." For each day, calculate the profit if you sell on that day.
  - Use a variable `minPrice` to track the current minimum buy price, and `maxProfit` to track the maximum profit. Update both variables each day to solve the problem in O(n) time.
starterCode:
  PYTHON: |
    class Solution:
        def maxProfit(self, prices: list[int]) -> int:
            pass
  C: |
    int maxProfit(int* prices, int pricesSize) {

    }
  CPP: |
    class Solution {
    public:
        int maxProfit(vector<int>& prices) {

        }
    };
  JAVASCRIPT: |
    /**
     * @param {number[]} prices
     * @return {number}
     */
    var maxProfit = function(prices) {

    };
testCases:
  - input: "prices = [7,1,5,3,6,4]"
    expected: "5"
    isHidden: false
    isKiller: false
  - input: "prices = [7,6,4,3,1]"
    expected: "0"
    isHidden: false
    isKiller: false
  - input: "prices = [1,2]"
    expected: "1"
    isHidden: false
    isKiller: false
  - input: "prices = [3,1,4,1,5,9,2,6]"
    expected: "8"
    isHidden: true
    isKiller: false
  - input: "prices = [1]"
    expected: "0"
    isHidden: true
    isKiller: false
  - input: "prices = [10000,9999,9998,9997,9996,9995,1,10000]"
    expected: "9999"
    isHidden: false
    isKiller: true
