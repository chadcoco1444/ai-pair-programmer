title: Longest Common Subsequence
slug: longest-common-subsequence
difficulty: MEDIUM
category: ALGORITHM
tags: [dynamic-programming, string]
concepts:
  - name: Dynamic Programming
    relevance: 0.9
  - name: Array
    relevance: 0.4
description: |
  Given two strings `text1` and `text2`, return the length of their **longest common subsequence**. If there is no common subsequence, return `0`.

  A **subsequence** of a string is a new string generated from the original string with some characters (can be none) deleted without changing the relative order of the remaining characters.

  - For example, `"ace"` is a subsequence of `"abcde"`, but `"aec"` is not.

  A **common subsequence** of two strings is a subsequence that is common to both strings.

  **Example 1:**
  ```
  Input: text1 = "abcde", text2 = "ace"
  Output: 3
  Explanation: The longest common subsequence is "ace" and its length is 3.
  ```

  **Example 2:**
  ```
  Input: text1 = "abc", text2 = "abc"
  Output: 3
  Explanation: The longest common subsequence is "abc" and its length is 3.
  ```

  **Example 3:**
  ```
  Input: text1 = "abc", text2 = "def"
  Output: 0
  Explanation: There is no such common subsequence, so the result is 0.
  ```

  **Constraints:**
  - `1 <= text1.length, text2.length <= 1000`
  - `text1` and `text2` consist of only lowercase English characters.

hints:
  - Build a 2D DP table: `dp[i][j]` represents the length of the longest common subsequence of `text1[0..i-1]` and `text2[0..j-1]`.
  - Transition: if `text1[i-1] == text2[j-1]`, then `dp[i][j] = dp[i-1][j-1] + 1`; otherwise `dp[i][j] = max(dp[i-1][j], dp[i][j-1])`.
  - Initialize row 0 and column 0 to all zeros (the LCS of an empty string with any string is 0); the final answer is `dp[m][n]`.

starterCode:
  PYTHON: |
    class Solution:
        def longestCommonSubsequence(self, text1: str, text2: str) -> int:
            # Write your solution here
            pass
  C: |
    int longestCommonSubsequence(char* text1, char* text2) {
        // Write your solution here
    }
  CPP: |
    #include <string>
    #include <vector>
    using namespace std;

    class Solution {
    public:
        int longestCommonSubsequence(string text1, string text2) {
            // Write your solution here
        }
    };
  JAVASCRIPT: |
    /**
     * @param {string} text1
     * @param {string} text2
     * @return {number}
     */
    var longestCommonSubsequence = function(text1, text2) {
        // Write your solution here
    };

testCases:
  - input: "\"abcde\" \"ace\""
    expected: "3"
    isHidden: false
    isKiller: false
  - input: "\"abc\" \"abc\""
    expected: "3"
    isHidden: false
    isKiller: false
  - input: "\"abc\" \"def\""
    expected: "0"
    isHidden: false
    isKiller: false
  - input: "\"oxcpqrsvwf\" \"shmtulqrypy\""
    expected: "2"
    isHidden: true
    isKiller: false
  - input: "\"bsbininm\" \"jmjkbkjkv\""
    expected: "1"
    isHidden: true
    isKiller: true
