title: Word Break
slug: word-break
difficulty: MEDIUM
category: ALGORITHM
tags: [dynamic-programming, hash-table, string]
concepts:
  - name: Dynamic Programming
    relevance: 0.9
  - name: Hash Table
    relevance: 0.6
  - name: Array
    relevance: 0.3
description: |
  给你一个字符串 `s` 和一个字符串列表 `wordDict` 作为字典。如果可以利用字典中出现的一个或多个单词拼接出 `s` 则返回 `true`。

  **注意：** 不要求字典中出现的单词全部都使用，并且字典中的单词可以重复使用。

  **示例 1：**
  ```
  输入：s = "leetcode", wordDict = ["leet","code"]
  输出：true
  解释：返回 true 因为 "leetcode" 可以由 "leet" 和 "code" 拼接成。
  ```

  **示例 2：**
  ```
  输入：s = "applepenapple", wordDict = ["apple","pen"]
  输出：true
  解释：返回 true 因为 "applepenapple" 可以由 "apple" "pen" "apple" 拼接成。
       注意，你可以重复使用字典中的单词。
  ```

  **示例 3：**
  ```
  输入：s = "catsandog", wordDict = ["cats","dog","sand","and","cat"]
  输出：false
  ```

  **约束条件：**
  - `1 <= s.length <= 300`
  - `1 <= wordDict.length <= 1000`
  - `1 <= wordDict[i].length <= 20`
  - `s` 和 `wordDict[i]` 仅由小写英文字母组成
  - `wordDict` 中的所有字符串**互不相同**

hints:
  - 定义 `dp[i]` 表示字符串 `s` 的前 `i` 个字符能否被字典中的单词拼接而成，初始化 `dp[0] = true`（空字符串可以被拼接）。
  - 对于每个位置 i，遍历所有 j < i，若 `dp[j]` 为 true 且 `s[j:i]` 在字典中，则 `dp[i] = true`。
  - 将 `wordDict` 转为哈希集合以实现 O(1) 的查找，可以显著提升效率。

starterCode:
  PYTHON: |
    from typing import List

    class Solution:
        def wordBreak(self, s: str, wordDict: List[str]) -> bool:
            # 请在此处编写你的解法
            pass
  C: |
    #include <stdbool.h>
    bool wordBreak(char* s, char** wordDict, int wordDictSize) {
        // 请在此处编写你的解法
    }
  CPP: |
    #include <string>
    #include <vector>
    using namespace std;

    class Solution {
    public:
        bool wordBreak(string s, vector<string>& wordDict) {
            // 请在此处编写你的解法
        }
    };
  JAVASCRIPT: |
    /**
     * @param {string} s
     * @param {string[]} wordDict
     * @return {boolean}
     */
    var wordBreak = function(s, wordDict) {
        // 请在此处编写你的解法
    };

testCases:
  - input: "\"leetcode\" [\"leet\",\"code\"]"
    expected: "true"
    isHidden: false
    isKiller: false
  - input: "\"applepenapple\" [\"apple\",\"pen\"]"
    expected: "true"
    isHidden: false
    isKiller: false
  - input: "\"catsandog\" [\"cats\",\"dog\",\"sand\",\"and\",\"cat\"]"
    expected: "false"
    isHidden: false
    isKiller: false
  - input: "\"aaaaaaa\" [\"aaaa\",\"aaa\"]"
    expected: "true"
    isHidden: true
    isKiller: false
  - input: "\"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaab\" [\"a\",\"aa\",\"aaa\",\"aaaa\",\"aaaaa\",\"aaaaaa\",\"aaaaaaa\",\"aaaaaaaa\",\"aaaaaaaaa\",\"aaaaaaaaaa\"]"
    expected: "false"
    isHidden: true
    isKiller: true
