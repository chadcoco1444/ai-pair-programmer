title: Word Break
slug: word-break
difficulty: MEDIUM
category: ALGORITHM
tags: [dynamic-programming, hash-table, string]
concepts:
  - name: Dynamic Programming
    relevance: 0.9
  - name: Hash Table
    relevance: 0.6
  - name: Array
    relevance: 0.3
description: |
  Given a string `s` and a dictionary of strings `wordDict`, return `true` if `s` can be segmented into a space-separated sequence of one or more dictionary words.

  **Note:** The same word in the dictionary may be reused multiple times in the segmentation, and you do not need to use all words in the dictionary.

  **Example 1:**
  ```
  Input: s = "leetcode", wordDict = ["leet","code"]
  Output: true
  Explanation: Return true because "leetcode" can be segmented as "leet code".
  ```

  **Example 2:**
  ```
  Input: s = "applepenapple", wordDict = ["apple","pen"]
  Output: true
  Explanation: Return true because "applepenapple" can be segmented as "apple pen apple".
               Note that you are allowed to reuse a dictionary word.
  ```

  **Example 3:**
  ```
  Input: s = "catsandog", wordDict = ["cats","dog","sand","and","cat"]
  Output: false
  ```

  **Constraints:**
  - `1 <= s.length <= 300`
  - `1 <= wordDict.length <= 1000`
  - `1 <= wordDict[i].length <= 20`
  - `s` and `wordDict[i]` consist of only lowercase English letters
  - All the strings of `wordDict` are **unique**

hints:
  - Define `dp[i]` as whether the first `i` characters of `s` can be segmented using words from the dictionary. Initialize `dp[0] = true` (an empty string can always be segmented).
  - For each position i, iterate over all j < i; if `dp[j]` is true and `s[j:i]` is in the dictionary, set `dp[i] = true`.
  - Convert `wordDict` to a hash set for O(1) lookups, which significantly improves performance.

starterCode:
  PYTHON: |
    from typing import List

    class Solution:
        def wordBreak(self, s: str, wordDict: List[str]) -> bool:
            # Write your solution here
            pass
  C: |
    #include <stdbool.h>
    bool wordBreak(char* s, char** wordDict, int wordDictSize) {
        // Write your solution here
    }
  CPP: |
    #include <string>
    #include <vector>
    using namespace std;

    class Solution {
    public:
        bool wordBreak(string s, vector<string>& wordDict) {
            // Write your solution here
        }
    };
  JAVASCRIPT: |
    /**
     * @param {string} s
     * @param {string[]} wordDict
     * @return {boolean}
     */
    var wordBreak = function(s, wordDict) {
        // Write your solution here
    };

testCases:
  - input: "\"leetcode\" [\"leet\",\"code\"]"
    expected: "true"
    isHidden: false
    isKiller: false
  - input: "\"applepenapple\" [\"apple\",\"pen\"]"
    expected: "true"
    isHidden: false
    isKiller: false
  - input: "\"catsandog\" [\"cats\",\"dog\",\"sand\",\"and\",\"cat\"]"
    expected: "false"
    isHidden: false
    isKiller: false
  - input: "\"aaaaaaa\" [\"aaaa\",\"aaa\"]"
    expected: "true"
    isHidden: true
    isKiller: false
  - input: "\"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaab\" [\"a\",\"aa\",\"aaa\",\"aaaa\",\"aaaaa\",\"aaaaaa\",\"aaaaaaa\",\"aaaaaaaa\",\"aaaaaaaaa\",\"aaaaaaaaaa\"]"
    expected: "false"
    isHidden: true
    isKiller: true
